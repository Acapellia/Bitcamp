#define PAN_SIZE 19
#include<stdio.h>
int team01(int pan[][PAN_SIZE], int* px, int* py)
{
	//*px = rand() % 5;
	//*py = rand() % 5;
	int x, y;
	scanf("%d %d", &x, &y);
	*px = x;
	*py = y;

	return 0;
}
